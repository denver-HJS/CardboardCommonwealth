'use strict';

describe('Controller: AboutCtrl', function () {

  beforeEach(module('cardboardCommonwealth'));

  var AboutCtrl;

  beforeEach(inject(function ($controller) {
    AboutCtrl = $controller('AboutCtrl', {});
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
